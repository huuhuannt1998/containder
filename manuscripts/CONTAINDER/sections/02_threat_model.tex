\section{Background and Threat Model}\label{sec:threat}

\subsection{Authorization in IEEE~2030.5 / CSIP}
% provenance: jrn_01KXT490ZD4444AWMRG5296GJA (threat model + CSIP function sets); lit passos2025tutorial for the protocol overview.
IEEE~2030.5 uses mutual TLS with X.509 device certificates. A device's identity is the
certificate-derived LFDI/SFDI, and its authorization is scoped by a
\texttt{FunctionSetAssignments} resource that binds it to one or more \texttt{DERProgram}s.
Physical control authority is carried by the \texttt{DERControlBase} control modes:
active-power capping (\texttt{opModMaxLimW}), signed fixed active power for storage
charge and discharge (\texttt{opModFixedW}), reactive power (\texttt{opModFixedVar},
\texttt{opModVoltVar}), volt-watt and frequency-watt curves, and the hard connect and
energize controls~\cite{passos2025tutorial}. The standard specifies certificate-based
authentication and function-set scoping; it does not mandate a single canonical
access-control object.

\subsection{Certificate validity and revocation}
% provenance: jrn_01KXT3JPAZ8RSGZK99X4G3HWPE (SAND2019-1490 4.1, primary-sourced).
The trust model has two properties central to this work~\cite{obert2019recommendations}.
Device certificates have indefinite lifetime, and revocation is prohibited: certificate
authorities are directed not to maintain revocation lists or run status responders, and
endpoints are directed not to check them. The stated reasons against online revocation are
concrete design constraints, not incidental: field devices have no guaranteed connectivity,
a hard-fail check destroys availability while a soft-fail check is trivially bypassed by
blocking the responder, revocation presupposes a secure time source that inverters may lack,
and no party is defined as responsible for reporting a compromised device. The sanctioned
substitute is a local allow or deny list. The consequence for our threat model is direct:
once a credential is compromised, there is no online mechanism to withdraw it, so its
authority persists until it expires.

\subsection{Three separated layers}
% provenance: jrn_01KXT490ZD4444AWMRG5296GJA.
We separate three layers that the baseline standard conflates.
\emph{L1, bootstrap identity}: the hardware-rooted device certificate.
\emph{L2, operational credential}: the credential that authorizes live sessions to an
aggregator or DER management system.
\emph{L3, authorization scope}: the server-side binding from an identity to the function
sets it may invoke.
In baseline IEEE~2030.5, L1 and L2 are the same long-lived certificate, with no attestation
gate and no revocation path. This conflation, not any single cryptographic weakness, is what
makes a compromise persist for the certificate lifetime.

\subsection{Compromise loci and adversary}
% provenance: jrn_01KXT490ZD4444AWMRG5296GJA.
We consider four compromise loci of increasing consequence: a single device, an edge
gateway, an aggregator or DER management system, and a utility-side issuing role. The
adversary obtains a valid credential at one locus (by key extraction, session theft, or a
misconfigured or compromised server granting scope) and issues protocol-conformant control
commands within that credential's authorization. The adversary does not break TLS or the
underlying cryptography. The utility-side issuing role is the ceiling on what any credential
discipline can bound, because it can subvert issuance itself; we treat it as the boundary of
the design's protection rather than a case the design defeats.

\subsection{Credential lifecycle and existing containment}
% provenance: jrn_01KXT3JPAZ8RSGZK99X4G3HWPE (SAND 4.1, sourced); jrn_01KXTVC4CT850YTVE5EM3RJDGS (verify vs 2030.5-2023).
The motivation depends on exact certificate-management behavior, which differs across editions:
IEEE~2030.5-2018~\cite{ieee20305-2018}, IEEE~2030.5-2023~\cite{ieee20305-2023} (which supersedes it,
published December 2024), and the profile in force~\cite{sunspec2018csip}. We separate four questions the literature tends to conflate: whether a
certificate can be cryptographically revoked; whether a server can locally deny an identity
despite a valid certificate; whether a device can be re-enrolled or re-keyed; and whether an
active session or an already-issued control remains effective after credential expiry. Lack of
CRL/OCSP bounds only the first.

\paragraph{The paper's primary open risk, stated plainly.}
\textbf{We were unable to obtain IEEE~2030.5-2023.} It is paywalled at roughly US\$255, it is not
included in the IEEE GET free-standards programme, and we found no publicly accessible document
that quotes its clause text; every free source that quotes 2030.5 quotes the 2018 edition. Our
premise --- that a device certificate's lifetime is effectively indefinite while online revocation
is unavailable --- is therefore evidenced against the \emph{2018} edition and the Sandia
analysis~\cite{obert2019recommendations}, not against the edition now in force. We consider this
the single largest threat to the paper's external validity and we do not minimise it.
Table~\ref{tab:standards} states, row by row, exactly what each claim rests on and what would
still change if the 2023 text differed. Two rows deserve the reader's attention before the rest of
the paper is read. \textbf{S5 is not supported by any source we could access}, and is contradicted
in spirit: the Sandia analysis lists dynamic re-provisioning as a \emph{recommendation}, i.e.\ a
capability judged absent, so we treat re-enrollment as something CONTAINDER must supply rather
than something the standard already offers. \textbf{S8 is the row most exposed to edition drift},
because vendor and reseller descriptions of the 2023 revision state that it eliminates the
requirements for mandatory DERControl modes, which is precisely the territory S8 occupies. Where
our argument depends on S5 or S8 we say so at the point of use.
\BLOCKER{PI: purchase IEEE 2030.5-2023 (IEEE Xplore doc 10785536, DOI 10.1109/IEEESTD.2024.10785536, approx.\ US\$255) and confirm S1--S8 clause by clause}

\begin{table}[t]
\centering
\caption{Every standards claim the argument uses, the accessible source of record for it, and what
remains unverified. \textbf{No row has been checked against IEEE~2030.5-2023}; the edition in force
could not be obtained (see text). SAND = Sandia SAND2019-1490~\cite{obert2019recommendations};
CSIP = Common Smart Inverter Profile Implementation Guide v2.1~\cite{sunspec2018csip}. ``Free
source'' means a document a reader can check without purchase.}
\label{tab:standards}
\footnotesize
\begin{tabular}{@{}p{0.04\columnwidth}p{0.32\columnwidth}p{0.23\columnwidth}p{0.26\columnwidth}@{}}
\toprule
ID & Claim & Free source of record & Residual risk \\
\midrule
S1 & Device certificate lifetime indefinite & SAND \S4.1, verbatim & 2023 clause unread \\
S2 & CRL use prohibited & SAND \S4.1, verbatim & 2023 clause unread \\
S3 & OCSP prohibited & SAND \S4.1, verbatim & 2023 clause unread \\
S4 & Local allow/deny listing available & SAND \S4.1, \S3.1.1.3; CSIP \S5.2.1.4 & permitted, not
required (SAND \S3.1.1.4) \\
S5 & Certificate replacement / re-enrollment possible & \textbf{none} & \textbf{unsupported}; SAND
Rec.~5 implies absent \\
S6 & LFDI/SFDI derives from certificate & SAND \S3.1.1.1 and CSIP \S5.2.1.2 (independent) &
\textbf{low}; two sources agree \\
S7 & FunctionSetAssignments scopes operations & CSIP \S5.2.3.1 & clause number only \\
S8 & Scheduled DER controls can persist & CSIP \S4.4.2, \S6.1.8.2 & \textbf{high}; 2023 reportedly
drops mandatory DERControl modes \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Assets, adversary, and goals}
% provenance: jrn_01KXTVC4CT850YTVE5EM3RJDGS (revision-directed threat-model structure).
\emph{Protected assets:} DER control integrity, fleet authorization, operational and attestation
keys, and feeder stability and service continuity. \emph{Adversary capabilities:} steals an
operational key (and, where tested, a bootstrap key), compromises firmware, steals an active
session, compromises a gateway or aggregator, issues protocol-conformant commands, and observes
authorized telemetry. \emph{Exclusions:} breaking standard cryptography, physical destruction, and
compromising both the root issuer and all audit controls, which we treat as the protection
boundary rather than a defended case. \emph{Security goals:} least authority, bounded new-session
authority, bounded active-session authority, bounded command-effect duration, fresh-key
containment, replay resistance, safe degradation, and auditability. \emph{Non-goals:} defending
against malicious root issuance without added governance, detecting every hardware attack,
replacing grid protection, and proving safety for every feeder.
