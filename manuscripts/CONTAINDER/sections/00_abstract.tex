% provenance: jrn_01KXT490ZD4444AWMRG5296GJA defines the four impact dimensions + threat model.
% provenance: jrn_01KXT49DNW5ZK6CEFDJW7CCK5M supports the separation claim.
% provenance: jrn_01KXT3JPAZ8RSGZK99X4G3HWPE (SAND2019-1490 4.1) supports revocation-infeasibility.
% provenance: dec_01KXT28WTZ92PJ7S8D3MME78D4 fixes the novelty framing (scope-bound attested ephemeral creds).
% provenance: experiments/results/scope_envelope.json (87.9 / 31.2, no containing reactive envelope);
% provenance: experiments/results/timeseries2.json (n=20 lifetime arms, recovery, tap-operation cost);
% provenance: jrn_01KXTX3PH43VR78AM1GZHC27T1 (invariants, 0.23 ms).
% NOTE: this file is input only by the (currently unbuilt) IEEE main.tex. main_els.tex carries its
% own copy of the abstract inline. Keep the two in sync -- a stale copy here is a landmine.
\begin{abstract}
IEEE~2030.5 binds a distributed energy resource's long-lived bootstrap identity to its
operational authorization and forbids online certificate revocation. A stolen credential
therefore retains control authority indefinitely, and no online mechanism can withdraw it. We
present CONTAINDER, which replaces the conflated device credential with attested, short-lived,
scope-bound operational credentials and uses non-renewal rather than revocation as the
containment mechanism, deployable incrementally through native and gateway-proxy tiers. Because
reachability alone does not distinguish these designs, we measure impact along four dimensions
--- cyber reach, state-dependent commandable flexibility, retained authority, and feeder
consequence --- and give a worked example of two compromises with identical reach but divergent
flexibility, persistence, and feeder outcome. We implement a credential service with real X.509
and mutual TLS and a real OpenDSS feeder pipeline. On the IEEE~8500-node feeder, ten implemented
security invariants pass and attested issuance costs about $0.23$\,ms. Our central physical
finding is negative and constrains the design space: sweeping the authorized envelope
continuously, \emph{no} reactive scope both contains a compromise and delivers the volt-var
service IEEE~1547-2018 requires. At the full Category~B envelope the induced overvoltage area is
$87.9$ p.u.-node with every seed tripping the overvoltage screen, and even at zero authorized
reactive injection it is $31.2$ with every seed tripping, because suppressing the absorption the
feeder relies on is itself an attack. Scope having failed, lifetime is the remaining control:
across twenty paired seeds a compromise at the conformant envelope holds the feeder in violation
for the whole horizon under a long-lived credential, whereas under CONTAINDER the feeder returns
to its pre-attack baseline within one minute of expiry with a settled residual of zero. We show
that integrated harm is the product of a measured accrual rate and the credential lifetime, so the
containment factor is a lifetime the operator chooses rather than an effect size we measured, and
we report its cost: a $51.5\%$ rise in regulator tap operations. These are reduced-scale pilot
results, reported as exploratory; the pre-registered confirmatory sweep is future work.
\end{abstract}

\begin{IEEEkeywords}
Critical infrastructure protection, distributed energy resources, IEEE~2030.5, remote attestation,
authorization, cyber-physical security.
\end{IEEEkeywords}
