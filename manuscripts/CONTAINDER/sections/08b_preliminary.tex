\section{Results}\label{sec:results}
% provenance: experiments/results/{authz_shape,shape_contrasts,lifecycle_physical,attackers}.json
% Confirmatory: hypotheses frozen in experiments/PREREGISTRATION_CONFIRMATORY.md, committed
% before any treatment arm ran (git f35c2e7); two amendments are recorded there and disclosed
% in the text below.

Results are organised by research question. Every physical estimate is paired by seed, every
interval is a bias-corrected percentile bootstrap at $10{,}000$ resamples, and no contrast is
reported as a median alone. The primary endpoint throughout is the two-sided ANSI~C84.1 Range~A
violation area $J_{\mathrm{band}} = \sum_n [\max(0, V_n - 1.05) + \max(0, 0.95 - V_n)]$ in
per-unit nodes, and $\Delta J_{\mathrm{band}}$ is its increment over the paired \emph{legitimate}
arm --- scheduled PV under the conformant IEEE~1547-2018 Category~B volt-var characteristic --- at
the same seed and operating state.

Two corrections to the harness precede everything reported here, because the pilot's central
comparison depended on both. Its two ``matched magnitude'' reactive primitives were not matched:
the curve arm's setpoint is per unit of \texttt{kvarmax} while the fixed-setpoint arm's is per
unit of apparent power, so at nominally identical labels they commanded $-2.15$ and $-5.28$\,kvar
per DER, a factor of $2.46$. And the conformant characteristic was written at $44\%$ of
\texttt{kvarmax} rather than $44\%$ of nameplate, so the legitimate counterfactual delivered less
than half the volt-var support it should have. Both are fixed by carrying every reactive quantity
in physical kvar; the corrected legitimate baseline at the pilot's light-load point falls from
$57.1$ to $46.1$ p.u.-node.

\subsection{RQ1 --- Credential-service correctness and overhead}
The credential service uses real X.509 / ECDSA certificates and an EAT-style attestation token.
Ten implemented security invariants pass (Table~\ref{tab:invariants}): a bootstrap identity cannot
authorize control, issuance requires fresh attestation over a freshly generated operational key,
replayed evidence and reused nonces are refused, an expired credential cannot issue a command,
scope is checked per command, a bad measurement yields only a narrowed safe-mode scope, a prior
epoch cannot self-renew, and a copied prior key fails after rotation. These are the properties the
prototype implements and tests; they are not a proof of the design.

On the workstation tier, over $500$ iterations, median software-path latencies are $0.03$\,ms for
key generation, $0.06$\,ms for attestation construction, $0.23$\,ms for a full attested issuance
and $2.3$\,ms for a real mutual-TLS handshake, and the issuer sustains about $4900$ renewals per
second single-threaded. \textbf{These are software-path timings and no hardware-backed measurement
exists.} We attempted one: on an Apple~M4 the Secure Enclave refuses key creation to a binary
without a code-signing entitlement this build could not obtain, and the platform's Swift toolchain
carries an SDK mismatch that prevents building against the Security framework. The attempt and its
failure are recorded in \texttt{results/hwkey.json}. Every cryptographic figure in this paper is
therefore a software-path figure, and the fresh-key containment claim rests on an
\emph{assumption} of non-exportable storage rather than a demonstration of it.

\begin{table}[t]
\centering
\caption{Security invariants implemented by the prototype (real X.509/mTLS). All ten pass; the
identifiers match \texttt{results/credsvc.json}.}
\label{tab:invariants}
\small
\begin{tabular}{@{}p{0.05\columnwidth}p{0.70\columnwidth}c@{}}
\toprule
ID & Invariant & Result \\
\midrule
I1 & Bootstrap identity cannot authorize control & pass \\
I2 & Issuance requires a fresh attestation & pass \\
I3 & Credential bound to a fresh public key & pass \\
I4 & Replayed evidence rejected & pass \\
I5 & Expired credential cannot issue a command & pass \\
I6 & Scope enforced per command & pass \\
I7 & Bad measurement yields degraded/denied scope & pass \\
I8 & Prior epoch cannot self-renew & pass \\
I9 & Copied prior key fails after rotation & pass \\
I10 & Active session/command bounded past expiry & pass \\
\bottomrule
\end{tabular}
\end{table}

\subsection{RQ2 --- Authorization shape, and the state that decides whether it matters}
% provenance: run_authz_shape.py -> authz_shape.json; analyze_shape.py -> shape_contrasts.json

\paragraph{Setup.} An authorization is a \emph{set}, and an adversary holding a credential scoped
to that set plays its worst admissible point. Each set therefore carries a seven-point grid fixed
in code before the run, and the reported harm is the maximum over that grid, with the argmax
retained. Four families are evaluated: Q1, a symmetric magnitude cap $|q| \le c$, which contains
zero absorption at every width; Q2, an absorption floor $q \in [-Q_b, -\varphi]$, which excludes
it; Q3, a tube around the conformant characteristic; and Q5, no remote reactive authority at all.
Every set is evaluated under both control primitives at the \emph{identical physical bound}, which
is the matched-feasible-set comparison the pilot did not perform. Two feeders, three penetration
rungs each, $20$ paired seeds, $2520$ arms.

\paragraph{Result: consequence tracks the support the feeder is drawing, not penetration.}
Table~\ref{tab:reliance} and Fig.~\ref{fig:reliance} are the central conditional result. The harm an adversary can do by
withdrawing reactive support scales with the reactive absorption the fleet was
\emph{actually delivering} under the conformant characteristic --- from $0.1$ p.u.-node where the
fleet is net injecting to $334$ where it absorbs $2.0$\,Mvar --- and that quantity is not a
function of penetration. IEEE~123 at $200\%$ penetration draws no absorption at all, because its
voltages sit inside the characteristic's deadband, and there the attack is worth nothing; the same
feeder at $1000\%$ draws $2.2$\,Mvar and the attack is worth $58$. The operational reading is
direct: an operator can measure the quantity that decides their exposure, and it is fleet reactive
absorption, not PV capacity.

\paragraph{The effect survives at a compliant operating point.} This was the pilot's largest
external-validity gap: its central result was measured where the legitimate feeder was already
$62\%$ out of band. On IEEE~8500 at the calibrated compliant limit, legitimate operation carries
a violation area of $0.015$ p.u.-node and the worst admissible point of a full-width symmetric cap
raises it to $7.5$ --- a factor of $500$ --- without crossing any IEEE~1547 screen threshold. The
effect is therefore not an artifact of an overstressed feeder. It is, however, an order of
magnitude smaller than at the stressed rungs, and no screen fires there.

\begin{table}[t]
\centering
\caption{Harm tracks the reactive support the feeder draws, not penetration. Worst admissible
point of the full-width symmetric cap Q1 under the curve primitive, $n{=}20$ paired seeds.
\emph{Legit.\ $Q$} is the fleet reactive output under the conformant characteristic (negative =
absorbing); \emph{compliant} is the calibrated criterion of \S\ref{sec:feeder}. Screen is the
fraction of seeds crossing an IEEE~1547 Category~II threshold that the paired legitimate case did
not cross.}
\label{tab:reliance}
\small
\begin{tabular}{@{}llrrrrr@{}}
\toprule
Feeder & Pen. & Legit.\ $J_{\mathrm{band}}$ & Compliant & Legit.\ $Q$ (kvar) &
$\Delta J_{\mathrm{band}}$ & Screen \\
\midrule
IEEE 123  & $200\%$  & $0.000$  & yes & $+42$   & $0.10$   & $0.20$ \\
IEEE 123  & $600\%$  & $0.449$  & no  & $-1249$ & $17.09$  & $0.80$ \\
IEEE 123  & $1000\%$ & $4.219$  & no  & $-2189$ & $58.14$  & $0.10$ \\
IEEE 8500 & $50\%$   & $0.015$  & yes & $-292$  & $7.50$   & $0.00$ \\
IEEE 8500 & $100\%$  & $4.679$  & no  & $-872$  & $221.15$ & $1.00$ \\
IEEE 8500 & $150\%$  & $24.468$ & no  & $-1979$ & $334.50$ & $1.00$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_reliance.pdf}
\caption{Induced $\Delta J_{\mathrm{band}}$ against the reactive absorption the fleet delivers
under the conformant characteristic, worst admissible point of the full-width symmetric cap,
$n{=}20$ paired seeds per point, bars are bootstrap $95\%$ intervals. Circled points are rungs at
which legitimate operation is compliant. Both feeders lie on the same relationship although their
penetrations at comparable harm differ by an order of magnitude, which is why absorption rather
than penetration is the quantity an operator should watch.}
\label{fig:reliance}
\end{figure}

\paragraph{Result: shape contains where width does not (H1).} At the exactly matched width of
$0.50\,Q_b$, a symmetric cap that admits zero absorption is worse than an absorption floor that
does not, on every IEEE~8500 rung under both primitives and on every IEEE~123 rung under the curve
primitive (Table~\ref{tab:h1}). On IEEE~8500 at $150\%$ the paired difference is $41.1$
p.u.-node, CI $[38.5, 43.3]$. The absorption floor achieves this at essentially no service cost:
its excess reactive-support deficit over the paired legitimate arm is $+0.07$ or less in every
supported cell, and the Q5 read-only arm --- which grants no remote authority and must by
construction cost nothing --- reads $+0.001$, which is the null control that validates the
measure.

H1 fails in one place, and we report it rather than aggregate it away: under the
\emph{fixed-setpoint} primitive on IEEE~123 the difference is not significant at two rungs and
reverses at the third ($-2.8$, CI $[-11.7, -1.9]$ at $600\%$). A fixed setpoint applies the same
kvar at every node irrespective of local voltage, so on a feeder whose voltage spread is narrow an
absorption floor can over-absorb at the low-voltage end and lose more on the undervoltage term
than it gains on the overvoltage term. The two-sided endpoint is what makes this visible; the
pilot's one-sided endpoint would have scored it as an improvement.

\paragraph{The pre-registered interpretation rule had to be replaced, and this is the disclosure.}
The frozen rule required the absorption-floor arm's reactive support deficit to stay below $25\%$
of conformant demand. That threshold presumes the metric reads near zero when nothing is wrong,
and it does not: the \emph{legitimate, unattacked} arm scores $0.53$ to $0.999$ across the ladder,
because the reference output is evaluated at a per-unit voltage computed from the bus base while
the inverter controller tracks its own rated-voltage reference under damped iteration. The rule is
therefore evaluated on the excess over the paired legitimate arm, with the same $25$-point
threshold. The substitution is post-hoc; the Q5 null control at $+0.001$ is the evidence that the
differential form measures what the absolute form could not.

\begin{table}[t]
\centering
\caption{H1 at exactly matched width ($0.50\,Q_b$): symmetric cap Q1 (admits zero absorption)
against absorption floor Q2 (excludes it). Paired by seed, $n{=}20$, bootstrap CI on the paired
difference. \emph{Excess deficit} is the Q2 arm's reactive-support deficit above its own paired
legitimate arm.}
\label{tab:h1}
\small
\begin{tabular}{@{}llrrrl@{}}
\toprule
Feeder & Pen. & Primitive & Q1 $-$ Q2 & 95\% CI & Excess deficit \\
\midrule
IEEE 123  & $200\%$  & curve    & $0.03$   & $[0.00, 0.06]$    & $-0.000$ \\
IEEE 123  & $600\%$  & curve    & $3.15$   & $[1.53, 17.53]$   & $+0.002$ \\
IEEE 123  & $1000\%$ & curve    & $37.08$  & $[26.85, 56.13]$  & $+0.000$ \\
IEEE 123  & $600\%$  & setpoint & $-2.81$  & $[-11.73, -1.90]$ & $+0.001$ \\
IEEE 8500 & $50\%$   & curve    & $1.48$   & $[1.14, 1.83]$    & $+0.146$ \\
IEEE 8500 & $100\%$  & curve    & $24.24$  & $[22.82, 25.60]$  & $+0.027$ \\
IEEE 8500 & $150\%$  & curve    & $41.12$  & $[38.48, 43.27]$  & $+0.071$ \\
IEEE 8500 & $150\%$  & setpoint & $36.38$  & $[33.31, 43.71]$  & $+0.018$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Result: the primitive is not neutral (H3 rejected).} We pre-registered the expectation
that, given matched feasible sets, the two primitives would not differ materially. They do: $43$
of $48$ matched sets show a paired difference whose CI excludes zero. The direction is not
constant --- the fixed setpoint is worse where forcing a uniform kvar at every node aggravates the
voltage spread, and better where it happens to over-absorb usefully --- which is precisely why the
effect is a property of \emph{how a bound interacts with the voltage profile} rather than of the
function set. The design recommendation is unchanged and does not depend on the primitive:
authorize a reactive band that excludes zero absorption. But the pilot's conclusion that the two
primitives ``behave the same way'' does not survive a matched comparison at $20$ seeds, and we
withdraw it.

\subsection{RQ3, RQ5, RQ6 --- What each containment layer is actually worth}
% provenance: run_lifecycle_physical.py -> lifecycle_physical.json

\paragraph{Setup.} A sixty-minute horizon with a compromise at minute five, stepped minute by
minute through a moving exogenous state: irradiance on a midday arc with a cloud transient placed
deliberately inside the post-expiry window, and load rising toward evening. Each arm is one
continuous session --- taps and capacitor states carry forward --- so whether the feeder recovers
is a measurement rather than a construction. The pilot's horizon was static, under which
integrated harm is exactly (accrual rate)~$\times$~(minutes honoured) and no lifetime comparison
can say anything a multiplication could not. Two feeders at their stressed rungs, $20$ paired
seeds, $22$ lifecycle arms each.

\paragraph{Result: identity denial alone contains nothing (Table~\ref{tab:lifecycle}).} This is
the paper's motivating claim, and it is now a measurement. Denying the compromised identity ---
the mechanism IEEE~2030.5 sanctions in place of revocation --- reduces integrated harm by
\textbf{exactly zero} (Fig.~\ref{fig:layers}) on both feeders, at both detection delays. The denied identity opens no new
session, and that is all it does: the adversary's established session keeps accepting commands and
its issued control keeps acting. Adding session termination reduces harm by $48.8\%$ on IEEE~8500
and $54.4\%$ on IEEE~123; adding command cancellation as well reaches $70.6\%$ and $76.3\%$.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_layers.pdf}
\caption{Integrated harm by containment layer, IEEE~8500 at $150\%$ penetration, $n{=}20$ paired
seeds, bars are bootstrap $95\%$ intervals and the dotted line is the long-lived baseline. Dark
bars are indistinguishable from that baseline: credential expiry alone, command cleanup alone, and
denying the compromised identity each remove no harm. Percentages are relative to the baseline.}
\label{fig:layers}
\end{figure}

\paragraph{Result: neither mechanism suffices alone, for two different reasons (H4).} The
mechanism ablation separates cleanly, which the pilot's harness could not do --- its activity
predicate mapped three lifecycles onto one arm, and the resulting claim was withdrawn. With
credential expiry but neither enforcement mechanism (S0), retained authority is
$(T_{\mathrm{cred}}, T_{\mathrm{sess}}, T_{\mathrm{cmd}}) = (25, 55, 55)$ minutes and the
integrated harm is \emph{identical to the long-lived baseline}: expiry alone contains nothing.
With command cleanup but no session enforcement (S2) the result is also identical to baseline, but
by a different route --- the adversary re-issues over its still-open session after every cleanup
sweep, accepting $17$ commands where the baseline needed $4$. Session enforcement alone (S1)
bounds the session but leaves a command overhang of one full control duration, $(25, 25, 30)$.
Only both together (S3) bound the effect at the cleanup latency, $(25, 25, 26)$.

The integral reductions are modest --- $9.4\%$ for S1 and $11.1\%$ for S3 on IEEE~8500 --- because
at a $25$-minute lifetime the adversary still holds the feeder for $25$ of the horizon's $55$
exposed minutes, and the integral is dominated by the pre-expiry period. That is the honest shape
of the mechanism: \textbf{it truncates the exposure window, it does not attenuate the excursion},
and the size of the benefit is set by the lifetime an operator chooses against the exposure window
their detection and response leaves open. The lifetime sweep makes this explicit: integrated harm
rises from $283$ p.u.-node-min at a five-minute lifetime through $392$, $494$, $858$ and $959$ to
the $965$ of the unbounded baseline at $55$ minutes.

\begin{table}[t]
\centering
\caption{Retained authority and integrated harm by containment layer, IEEE~8500 at $150\%$
penetration, $n{=}20$ paired seeds, sixty-minute horizon with compromise at minute five.
$(T_{\mathrm{cred}}, T_{\mathrm{sess}}, T_{\mathrm{cmd}})$ in minutes from the compromise;
integral in p.u.-node-min with bootstrap CI. \emph{Cmds} is the number of adversarial controls
accepted.}
\label{tab:lifecycle}
\small
\begin{tabular}{@{}lrrrrlrr@{}}
\toprule
Arm & $T_{\mathrm{cred}}$ & $T_{\mathrm{sess}}$ & $T_{\mathrm{cmd}}$ & Integral & 95\% CI &
Cmds & vs base \\
\midrule
Long-lived baseline            & $\infty$ & 55 & 55 & $964.97$ & $[936.8, 1037.2]$ & 4  & --- \\
S0 expiry only                 & 25 & 55 & 55 & $964.97$ & $[936.8, 1037.2]$ & 4  & $0.0\%$ \\
S1 session enforcement         & 25 & 25 & 30 & $874.40$ & $[822.0, 941.0]$  & 2  & $-9.4\%$ \\
S2 command cleanup only        & 25 & 55 & 55 & $964.97$ & $[936.8, 1037.2]$ & 17 & $0.0\%$ \\
S3 both (CONTAINDER)           & 25 & 25 & 26 & $858.04$ & $[788.2, 911.9]$  & 2  & $-11.1\%$ \\
\midrule
Denylist only, detect $+5$     & 5  & 55 & 55 & $964.97$ & $[936.8, 1037.2]$ & 4  & $0.0\%$ \\
\quad $+$ session termination  & 5  & 5  & 15 & $494.45$ & $[439.2, 549.8]$  & 1  & $-48.8\%$ \\
\quad $+$ command cancellation & 5  & 5  & 6  & $283.41$ & $[213.9, 355.3]$  & 1  & $-70.6\%$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Operational cost.} Bounded lifetime is not free. Regulator tap operations rise from a
median of $49$ under the unbounded baseline to $56$ under S3, and the transition at expiry is what
causes them: the cost is incurred by the containment event, not by the attack. An operator
choosing a short lifetime is buying truncated exposure with regulator wear, and the voltage
endpoint does not price it.

\subsection{RQ4 --- When lifetime binds, and when detection does}
% provenance: run_attackers.py -> attackers.json

\paragraph{Setup.} The pilot named this its largest gap: an excursion that puts most of a feeder
out of band would be surfaced by AMI reporting within a metering interval, so the operative
containment latency is the utility's detection time, not the credential lifetime. Bounded lifetime
binds only when detection does not. Five attackers share one operating state and one authorized
set, differing only in what they optimise and what they must stay below. The action space is
two-dimensional --- the reactive point and the active-power fraction --- because a stealth
attacker trades one against the other: withdrawing absorption raises voltage and curtailing active
power lowers it, so a mixture can hold voltage inside the band while still doing damage. An attack
is taken to be \emph{voltage-detectable} within a metering interval if it crosses an IEEE~1547
screen threshold or adds more than ten nodes above $1.05$\,p.u.\ over the paired legitimate case;
this is a declared proxy for AMI-based detection, not a claim about any utility's monitoring.

\paragraph{Result: the loud attacker is bounded by detection, and the quiet one is not.}
Table~\ref{tab:attackers} gives the crossover. The oracle attacker achieves the largest voltage
harm on IEEE~8500 at $150\%$ penetration --- $\Delta J_{\mathrm{band}} = 406$ p.u.-node --- and is
detectable in every seed, crossing a protection screen in every seed. Against that adversary the
credential lifetime is irrelevant: any operator watching voltage sees it within one interval, and
the containment latency is their response time.

The stealth-constrained attacker is the case the pilot did not evaluate, and it inverts the
picture. Constrained to cross no screen and to add no nodes above band, it still accrues
$\Delta J_{\mathrm{band}} = 14.1$ p.u.-node while the count of over-band nodes \emph{falls} by
$180$ relative to legitimate operation. It does this by curtailing a quarter of fleet output and
holding reactive support at zero, moving the violation onto the undervoltage side of the band,
where a one-sided overvoltage endpoint --- the pilot's endpoint --- scores it as an improvement.

\paragraph{Result: economic harm is entirely invisible to a voltage detector.} The wear and
curtailment attackers make the point sharply. Subject to the same stealth constraint, both settle
on full absorption at zero active power: they curtail \textbf{$100\%$ of fleet generation} while
\emph{reducing} the voltage violation area by $24.5$ p.u.-node, and are detectable in no seed on
either feeder. A voltage-based detector does not merely miss this attack; it reads the feeder as
healthier than legitimate operation. Nothing in the operator's voltage telemetry will ever end it,
so the only bound on its duration is the one the credential imposes. \textbf{This is the regime in
which bounded lifetime is the binding containment mechanism, and it is the regime an evaluation
using only a loud attacker and a one-sided endpoint cannot see.}

\begin{table}[t]
\centering
\caption{Attacker models at IEEE~8500, $150\%$ penetration, $n{=}20$ paired seeds. \emph{Detect}
is the fraction of seeds the declared AMI proxy flags within one metering interval;
\emph{Curtail} is the fraction of available fleet active energy withheld. Negative
$\Delta J_{\mathrm{band}}$ means the attack left the feeder with \emph{less} voltage violation
than legitimate operation while doing economic harm.}
\label{tab:attackers}
\small
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Attacker & $\Delta J_{\mathrm{band}}$ & $\Delta$ nodes over & Detect & Curtail & Taps \\
\midrule
A1 oracle              & $405.9$  & $+302$   & $1.00$ & $19\%$  & 46 \\
A2 telemetry-limited   & $306.4$  & $+1603$  & $1.00$ & $0\%$   & 57 \\
A3 stealth-constrained & $14.1$   & $-180$   & $0.00$ & $25\%$  & 15 \\
A4 wear                & $-24.5$  & $-3568$  & $0.00$ & $100\%$ & 42 \\
A5 curtailment         & $-24.5$  & $-3568$  & $0.00$ & $100\%$ & 42 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{What this does not settle.} The crossover point is a function of the detection proxy.
A utility correlating fleet reactive output or generation against forecast would catch the
curtailment attacker immediately, and the argument above would not apply to it. What the result
establishes is narrower and still useful: \emph{voltage} monitoring, which is the telemetry a
distribution operator is most likely to have, is blind to a compromise that does its damage in
curtailed energy, and against that compromise credential lifetime is the only bound we measured.

\subsection{Threats to validity}\label{sec:limits}
We report what this evaluation does not establish.

\emph{No hardware.} Every cryptographic figure is a software-path timing on a workstation. The
Secure Enclave attempt failed for the entitlement reason above, and no TPM was available. The
fresh-key containment property assumes non-exportable storage rather than demonstrating it.

\emph{No third-party interoperability.} The IEEE~2030.5 client is ours. Nothing here demonstrates
that an unmodified commercial endpoint behaves as modelled, and the session and command-cleanup
semantics we enforce are ones the accessible standards material does not specify --- which is why
we enforce them, but also why no conformance claim follows.

\emph{One solver.} OpenDSS only; no GridLAB-D cross-check was run. The two feeders differ in
voltage level, topology, regulator structure and stiffness, and the direction of every primary
contrast agrees across them, but a second solver would test the model rather than the feeder.

\emph{The standard's current edition is unread.} IEEE~2030.5-2023 is paywalled and was not
obtained. Table~\ref{tab:standards} states which claims depend on it.

\emph{Detection is modelled, not observed.} The stealth analysis assumes an AMI-based detector
with a declared node-count threshold. It is a proxy, and the crossover between lifetime-bound and
detection-bound containment moves with it.

\emph{Two disclosed departures from the pre-registration.} The penetration ladder lost its top
rung and the attacker grid two points, before any result was seen, because the strong-injection
arms exhausted the solver retry budget. And the IEEE~123 fleet was initially set to that feeder's
exact load-bus count, so every seed drew the same buses and the twenty IEEE~123 seeds were one
circuit; the arms were re-run at half the fleet after the defect was found in the first results.
Both are recorded in the pre-registration with the reasoning above.
